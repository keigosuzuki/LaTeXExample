%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Here is common used preamble
% write as : \usepackage{preamble_common} after \documentclass
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\usepackage{booktabs}
\usepackage[dvipdfmx]{graphicx,color}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{bm}
\usepackage{ascmac}
\usepackage{newtxtext}
\usepackage{verbatim}
\usepackage{subfigure}
\usepackage{mathtools} % for using \dcases

\usepackage{multicol}
\usepackage{pifont}
%\usepackage{docmute} % for individual compile in Thesis

\renewcommand{\bf}{\bfseries}
\renewcommand{\gt}{\gtfamily}
\renewcommand{\sf}{\sffamily}
\label{alg1}
\newcommand{\eq}[1]{(\ref{eq:#1})}
\newcommand{\zu}[1]{\fig{#1}}
\newcommand{\shiki}[1]{式(\ref{eq:#1})} % for japanese text only
\newcommand{\fig}[1]{Fig.\ \ref{fig:#1}}
\newcommand{\grp}[1]{Fig.\ \ref{grp:#1}}
\newcommand{\tb}[1]{Tab.\ \ref{tb:#1}}
\newcommand{\defeq}{:=}
\newcommand{\bvec}[1]{\mbox{\boldmath $#1$}}
\newcommand{\tvec}[1]{\bvec{#1}^{\mathsf{T}}}

%% Define
\newtheorem{theorem}{Theorem}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{corollary}[theorem]{Corollary}

% for Graphic path
\graphicspath{{./fig/}} %in the current \fig folder
% \graphicspath{{../fig/}} %in the upper \fig folder
